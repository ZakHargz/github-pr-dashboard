# Dependencies — will be built fresh in Docker
# node_modules

# Build output — must be pre-built locally with `npm run build`
# .next

# Source control
.git
.gitignore

# Docker artefacts themselves
Dockerfile
.dockerignore

# Local env files (never bake secrets into image)
.env
.env.*
!.env.example

# Editor / OS noise
.DS_Store
*.swp
*.swo
.idea
.vscode

# Test / CI output
coverage
